# build artifacts
/homey
/dist/

# local data (SQLite database)
/data/
*.db
*.db-wal
*.db-shm

# web UI (Phase 4)
web/node_modules/
web/.svelte-kit/
web/build/

# environment files
.env

# local agent context overrides (personal, never commit)
AGENTS.override.md

# macOS
.DS_Store

# dev.sh token file (development only)
*.db.token
