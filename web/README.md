# homey Web UI

The Web UI (dashboard, rooms, containers, items, search, themes) arrives in
Phase 4 of the roadmap. Planned stack: Svelte 5 + Vite + TypeScript, built
and embedded into the Go binary so deployment stays a single container.
