# Deployment examples

The base `Dockerfile` and `compose.yaml` live at the repository root.

Example deployments will be documented here: reverse proxy with HTTPS
(Caddy/Traefik), NAS-specific notes, Raspberry Pi, and multi-arch builds.
